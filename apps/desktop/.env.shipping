# The shipping build: the webview talks to its own bundled sidecar, not the
# dev scaffolding on :8080. Loaded by `vite build --mode shipping`
# (npm run build:shipping); plain `npm run build` keeps the dev default.
VITE_AULAR_API=http://127.0.0.1:8787
